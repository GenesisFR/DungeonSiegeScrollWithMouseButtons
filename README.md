# DungeonSiegeScrollWithMouseButtons
An AutoHotkey 2 script for _Dungeon Siege 1_ to scroll with the mouse buttons, made for Eksevis.

## Usage

When the script is running, toggling on `CapsLock` will allow the mouse wheel or side buttons to scroll the console in `DSMod`/`DSLOAMod`.
